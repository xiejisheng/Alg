package com.xjs.alg.test;

public class Kmp {
	public static void main(String[] args) {
		String input = "abcab";
		char[] arr = input.toCharArray();
		kmp(arr);
	}

	// 类回文
	private static void kmp(char[] arr) {
		int[] t = new int[arr.length];
		int i = 0;
		int j = arr.length;
		while (i <= j) {
			if (arr[]) {
				
			}
		}
	}
}
